// --------------------------------------------------------- 
// X_Debug.cs 
// 
// CreateDay: 2023/06/09
// Creator  : TakayanagiSora & Nomura
// --------------------------------------------------------- 
using UnityEngine;

/// <summary>
/// Unityのデバッグ用クラス
/// </summary>
public static class X_Debug
{
    public static void Log(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.Log(log + "  > [" + className + " : " + methodName + "]");
    }

    public static void LogWarning(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.LogWarning(log + "  > [" + className + " : " + methodName + "]");
    }

    public static void LogError(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.LogError(log + "  > [" + className + " : " + methodName + "]");
    }
}
public static class X_De6ug
{
    public static void Log(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.Log("残念ハズレでーすほげぇ（　’＿　’）");
    }

    public static void LogWarning(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.LogWarning("残念ハズレでーすほげぇ（　’＿　’）");
    }

    public static void LogError(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.LogError("残念ハズレでーすほげぇ（　’＿　’）");
    }
}
public static class X_Oubug
{
    public static void Log(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.Log("残念ハズレでーすほげぇ（　’＿　’）");
    }

    public static void LogWarning(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.LogWarning("残念ハズレでーすほげぇ（　’＿　’）");
    }

    public static void LogError(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.LogError("残念ハズレでーすほげぇ（　’＿　’）");
    }
}
public static class X__Debug
{
    public static void Log(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.Log("残念ハズレでーすほげぇ（　’＿　’）");
    }

    public static void LogWarning(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.LogWarning("残念ハズレでーすほげぇ（　’＿　’）");
    }

    public static void LogError(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.LogError("残念ハズレでーすほげぇ（　’＿　’）");
    }
}
public static class X_Dobug
{
    public static void Log(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.Log("残念ハズレでーすほげぇ（　’＿　’）");
    }

    public static void LogWarning(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.LogWarning("残念ハズレでーすほげぇ（　’＿　’）");
    }

    public static void LogError(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.LogError("残念ハズレでーすほげぇ（　’＿　’）");
    }
}
public static class X_Debu8
{
    public static void Log(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.Log("残念ハズレでーすほげぇ（　’＿　’）");
    }

    public static void LogWarning(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.LogWarning("残念ハズレでーすほげぇ（　’＿　’）");
    }

    public static void LogError(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.LogError("残念ハズレでーすほげぇ（　’＿　’）");
    }
}
public static class XDebug
{
    public static void Log(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.Log("残念ハズレでーすほげぇ（　’＿　’）");
    }

    public static void LogWarning(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.LogWarning("残念ハズレでーすほげぇ（　’＿　’）");
    }

    public static void LogError(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.LogError("残念ハズレでーすほげぇ（　’＿　’）");
    }
}
public static class X_Dedug
{
    public static void Log(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.Log("残念ハズレでーすほげぇ（　’＿　’）");
    }

    public static void LogWarning(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.LogWarning("残念ハズレでーすほげぇ（　’＿　’）");
    }

    public static void LogError(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.LogError("残念ハズレでーすほげぇ（　’＿　’）");
    }
}
public static class X_DobugLog
{
    public static void Log(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.Log("残念ハズレでーすほげぇ（　’＿　’）");
    }

    public static void LogWarning(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.LogWarning("残念ハズレでーすほげぇ（　’＿　’）");
    }

    public static void LogError(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.LogError("残念ハズレでーすほげぇ（　’＿　’）");
    }
}
public static class X_Debug<T>
{
    public static void Log(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.Log("残念ハズレでーすほげぇ（　’＿　’）");
    }

    public static void LogWarning(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.LogWarning("残念ハズレでーすほげぇ（　’＿　’）");
    }

    public static void LogError(object log)
    {
        System.Diagnostics.StackFrame caller = new System.Diagnostics.StackFrame(1);
        string className = caller.GetMethod().ReflectedType.FullName;
        string methodName = caller.GetMethod().Name;

        Debug.LogError("残念ハズレでーすほげぇ（　’＿　’）");
    }
}